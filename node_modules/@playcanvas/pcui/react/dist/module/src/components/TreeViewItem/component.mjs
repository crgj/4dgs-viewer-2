import { Element } from '../Element/component.mjs';
import { TreeViewItem as TreeViewItem$1 } from './index.mjs';

/**
 * Represents a Tree View Item to be added to a TreeView.
 */
class TreeViewItem extends Element {
    constructor(props) {
        super(props);
        this.onSelect = () => {
            if (props.onSelect) {
                props.onSelect(() => {
                    this.element.selected = false;
                });
            }
        };
        if (props.onDeselect) {
            this.onDeselect = props.onDeselect;
        }
        this.onAttach = this.onAttachFn.bind(this);
    }
    onAttachFn() {
        if (this.props.onSelect) {
            this.element.on('select', this.onSelect);
        }
        if (this.props.onDeselect) {
            this.element.on('deselect', this.onDeselect);
        }
    }
}
TreeViewItem.ctor = TreeViewItem$1;

export { TreeViewItem };
//# sourceMappingURL=component.mjs.map
